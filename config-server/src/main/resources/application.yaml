server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/akhil3kumar/Hospital_Management_Api.git
          search-paths: config-server
          clone-on-start: true
